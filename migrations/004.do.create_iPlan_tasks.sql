CREATE TABLE tasks (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    task TEXT NOT NULL,
    member INTEGER REFERENCES members(id) ON DELETE RESTRICT NOT NULL,
    trip INTEGER REFERENCES trips(id) ON DELETE RESTRICT NOT NULL,
    list INTEGER REFERENCES lists(id) ON DELETE CASCADE NOT NULL
);